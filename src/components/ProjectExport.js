'use client';
import {useState} from 'react';
export default function ProjectExport({projectId}){
 const [open,setOpen]=useState(false),[columns,setColumns]=useState([]),[selected,setSelected]=useState([]),[busy,setBusy]=useState(false),[error,setError]=useState(''),[total,setTotal]=useState(0);
 async function show(){setOpen(true);setError('');setBusy(true);try{const res=await fetch(`/api/projects/${projectId}/export`);const data=await res.json();if(!res.ok)throw new Error(data.error);setColumns(data.columns);setSelected(data.columns.map(c=>c.key));setTotal(data.total);}catch(e){setError(e.message);}finally{setBusy(false);}}
 async function download(){setBusy(true);setError('');try{const res=await fetch(`/api/projects/${projectId}/export`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({columns:selected})});if(!res.ok)throw new Error((await res.json()).error);const url=URL.createObjectURL(await res.blob());const a=document.createElement('a');a.href=url;a.download='phan-hoi-du-an.xlsx';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);setOpen(false);}catch(e){setError(e.message);}finally{setBusy(false);}}
 return <><button className="btn btn-secondary" onClick={show}>Xuất Excel (.xlsx)</button>{open&&<div className="modal-overlay"><div className="modal-content" role="dialog" aria-modal="true" aria-label="Chọn mục xuất Excel" style={{maxWidth:700,width:'calc(100vw - 2rem)'}}><div className="modal-header"><h2>Chọn mục xuất Excel</h2><button className="btn btn-ghost" disabled={busy} onClick={()=>setOpen(false)}>Đóng</button></div><div className="modal-body"><p>Xuất toàn bộ {total} phản hồi của dự án, gồm các mục bạn chọn.</p>{error&&<p role="alert" style={{color:'var(--danger)'}}>{error}</p>}<div className="flex gap-2" style={{margin:'1rem 0'}}><button className="btn btn-secondary btn-sm" disabled={busy} onClick={()=>setSelected(columns.map(c=>c.key))}>Chọn tất cả</button><button className="btn btn-secondary btn-sm" disabled={busy} onClick={()=>setSelected([])}>Bỏ chọn tất cả</button></div><div style={{maxHeight:'45vh',overflowY:'auto'}}>{columns.map(c=><label key={c.key} style={{display:'flex',gap:10,padding:8}}><input type="checkbox" checked={selected.includes(c.key)} disabled={busy} onChange={e=>setSelected(e.target.checked?[...selected,c.key]:selected.filter(k=>k!==c.key))}/>{c.label}</label>)}</div></div><div className="modal-footer"><button className="btn btn-primary" disabled={busy||!selected.length} onClick={download}>{busy?'Đang xử lý…':`Xuất ${selected.length} mục`}</button></div></div></div>}</>;
}
